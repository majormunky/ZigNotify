[Unit]
Description=munknotify - Zig notification daemon
Documentation=https://github.com/majormunky/munknotify
# Make sure we start after the graphical session is ready
After=graphical-session.target
PartOf=graphical-session.target

[Service]
Type=simple
ExecStart=%h/.local/bin/munknotify
Restart=on-failure
RestartSec=3

# Make sure the old daemon is gone before starting
ExecStartPre=/bin/sh -c 'pkill munknotify || true'

# Environment
Environment=WAYLAND_DISPLAY=wayland-1
Environment=XDG_RUNTIME_DIR=/run/user/%U

[Install]
WantedBy=graphical-session.target
